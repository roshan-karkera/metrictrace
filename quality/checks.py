"""Individual checks: row count vs rolling baseline, freshness, nulls, schema drift.

Each returns a verdict plus the expected and actual values, so a failure is
explainable rather than just red.
"""
